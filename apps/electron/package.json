{
  "name": "pano-cloud-viewer-electron",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev":   "electron .",
    "build": "tsc -p tsconfig.json && electron-builder",
    "pack":  "electron-builder --dir"
  },
  "dependencies": {
    "electron-updater": "^6.1.0"
  },
  "devDependencies": {
    "@types/node":   "^22.0.0",
    "electron":      "^32.0.0",
    "electron-builder": "^25.0.0",
    "typescript":    "^5.6.0"
  }
}
