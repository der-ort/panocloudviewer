node_modules/
dist/
!packages/viewer/dist/
!packages/core/dist/
out/
.next/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Environment
.env
.env.local

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/

# VitePress build cache
docs/.vitepress/cache/

# Local input data (E57 scans, raw point clouds, conversion inputs) — never commit
input_files/
# Local point-cloud datasets dropped into the demo for verification — never commit
apps/web/public/hahnensteig/
apps/*/public/_local/
# Real capture datasets dropped into the example app (point clouds + panoramas).
# Every subfolder of the example's public dir is local verification data — none
# of it should be committed. Keep the folder itself with a .gitkeep if needed.
apps/example/public/*/
